spring.application.name: test.yaml

default-value-ref: app-test

# 设置 runner 收集顺序
command-line-runner:
  collection: "[*app.MyRunner]"

# 设置 starter 收集顺序
application-event:
  collection: "[*,web-server-starter]"