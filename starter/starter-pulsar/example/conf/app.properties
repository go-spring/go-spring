spring.pulsar.a.url=pulsar://127.0.0.1:6650
spring.pulsar.a.fail-fast=true
# The default standalone bootstrap creates the public/default namespace, so a
# non-partitioned topic lookup succeeds even when the topic does not yet exist.
# Point at the topic the example actually uses to guarantee the probe reaches
# the broker without side effects.
spring.pulsar.a.health-check-topic=persistent://public/default/hello
# Expose the client's native Prometheus metrics on http://127.0.0.1:9091/metrics.
spring.pulsar.a.metrics.enabled=true
spring.pulsar.a.metrics.port=9091

spring.pulsar.b.url=pulsar://127.0.0.1:6650
spring.pulsar.b.fail-fast=true
spring.pulsar.b.health-check-topic=persistent://public/default/hello

# Authentication examples (disabled: dev standalone runs without auth).
# spring.pulsar.a.token=eyJhbGci...
# spring.pulsar.a.token-from-file=false
# spring.pulsar.a.token=/etc/pulsar/token.jwt
# spring.pulsar.a.token-from-file=true

# TLS examples (disabled: dev standalone runs plaintext on 6650).
# spring.pulsar.a.url=pulsar+ssl://broker:6651
# spring.pulsar.a.tls-trust-certs-file=/etc/pulsar/certs/ca.pem
# spring.pulsar.a.tls-cert-file=/etc/pulsar/certs/client.pem
# spring.pulsar.a.tls-key-file=/etc/pulsar/certs/client.key
# spring.pulsar.a.tls-allow-insecure=false
# spring.pulsar.a.tls-validate-hostname=true
