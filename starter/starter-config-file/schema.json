{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.config.import (file://...)",
  "description": "Local file configuration provider. Configured via spring.config.import URL.",
  "type": "object",
  "properties": {
    "file-url": {
      "type": "string",
      "description": "URL format: file:///path/to/config.[yaml|json|toml|properties]"
    }
  }
}