# One migration entry named "app". The starter matches this entry to the
# *gorm.DB bean "app" and the migration.Source bean "app" the application
# registers. db-ref is optional here since only one *gorm.DB bean exists, but it
# is shown for discoverability.
spring.migration.app.db-ref=app
spring.migration.app.enabled=true
