# Thrift server bind address.
spring.thrift.server.addr=:9292
spring.thrift.server.transport=framed

# Enable tracing and metrics middleware (off by default in some setups).
spring.thrift.server.interceptor.tracing.enabled=true
spring.thrift.server.interceptor.metrics.enabled=true

# Observability: OTLP gRPC trace exporter → Jaeger on :4317.
spring.observability.enable=true
spring.observability.service-name=thrift-otel-example
spring.observability.trace.exporter=otlp-grpc
spring.observability.trace.endpoint=127.0.0.1:4317
spring.observability.trace.insecure=true
spring.observability.trace.sampler-ratio=1.0

# Observability: Prometheus metrics on dedicated port :9090.
spring.observability.metrics.exporter=prometheus
spring.observability.metrics.port=9090
spring.observability.metrics.path=/metrics