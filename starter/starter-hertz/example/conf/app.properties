# Hertz drives its own HTTP server on 127.0.0.1:8003 (see example.go),
# so the built-in Go-Spring HTTP server must be disabled to avoid a
# port clash and duplicate lifecycle management. Unlike starter-gin/echo,
# the listen port is set on the Hertz engine (WithHostPorts), not here.
spring.http.server.enabled=false
