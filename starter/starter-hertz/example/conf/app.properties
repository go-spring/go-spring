# Hertz drives its own HTTP server on 127.0.0.1:9090 (see example.go),
# so the built-in Go-Spring HTTP server must be disabled to avoid a
# port clash and duplicate lifecycle management.
spring.http.server.enabled=false
