# This example is a self-contained smoke test for the resilience seams
# (rate limiting, circuit breaking, retry) built on the sentinel driver.
# It exercises the seams purely via API calls — no Go-Spring container or
# external services are required, so no spring.* properties are needed.

