{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.session.redis",
  "type": "object",
  "description": "Two buckets: spring.session.redis.default holds family-wide values (each overridable per instance), spring.session.redis.instances.<name> holds one entry per named instance. No other key belongs directly under spring.session.redis.",
  "properties": {
    "instances": {
      "type": "object",
      "description": "One entry per instance; the key is the bean name, and any name is legal.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "client": {
            "type": "string"
          },
          "key-prefix": {
            "type": "string",
            "default": "session:"
          }
        }
      }
    }
  }
}
