{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.outbox",
  "type": "object",
  "description": "Two buckets: spring.outbox.default holds family-wide values (each overridable per instance), spring.outbox.instances.<name> holds one entry per named instance. No other key belongs directly under spring.outbox.",
  "properties": {}
}
