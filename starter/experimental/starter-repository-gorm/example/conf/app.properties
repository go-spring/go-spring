# starter-repository-gorm needs no configuration: a repository.Repository is
# built in code with reposgorm.For over a *gorm.DB. This example opens its own
# in-memory sqlite database, so there is nothing to configure here — the file
# exists only so gs has a config directory to load.
