{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.lua.filter",
  "type": "object",
  "description": "Two buckets: spring.lua.filter.default holds family-wide values (each overridable per instance), spring.lua.filter.instances.<name> holds one entry per named instance. No other key belongs directly under spring.lua.filter.",
  "properties": {
    "instances": {
      "type": "object",
      "description": "One entry per instance; the key is the bean name, and any name is legal.",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "script": {
            "type": "string"
          }
        }
      }
    }
  }
}
