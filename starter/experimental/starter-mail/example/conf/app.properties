# The `notify` mailer points at the local MailHog SMTP server, which speaks
# plaintext SMTP with no auth on port 1025 — so TLS and authentication stay off
# here. A real deployment would use a submission host, e.g.:
#   spring.mail.instances.notify.host=smtp.example.com
#   spring.mail.instances.notify.port=587
#   spring.mail.instances.notify.username=apikey
#   spring.mail.instances.notify.password=${SMTP_PASSWORD}
#   spring.mail.instances.notify.tls.mode=starttls
spring.mail.instances.notify.host=127.0.0.1
spring.mail.instances.notify.port=1025
spring.mail.instances.notify.from=noreply@example.com
spring.mail.instances.notify.tls.mode=none
