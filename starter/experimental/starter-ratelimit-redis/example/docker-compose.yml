services:
  # Redis backing both the *redis.Client bean (starter-go-redis) and the
  # shared token bucket built on top of it (starter-ratelimit-redis).
  redis:
    image: redis:7
    container_name: starter-ratelimit-redis
    ports:
      - "127.0.0.1:6379:6379"
