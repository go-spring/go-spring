# One migration entry named "app", matched to the *gorm.DB bean "app".
# db-ref is optional here since only one *gorm.DB bean exists.
spring.migration.app.db-ref=app
spring.migration.app.dir=./sql
spring.migration.app.enabled=true
