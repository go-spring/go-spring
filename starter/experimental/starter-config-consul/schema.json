{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.config.import (consul://...)",
  "description": "Consul KV remote configuration provider. Configured via spring.config.import URL.",
  "type": "object",
  "properties": {
    "consul-url": {
      "type": "string",
      "description": "URL format: consul://host:port/path/to/key?scheme={http|https}&token=xxx"
    }
  }
}