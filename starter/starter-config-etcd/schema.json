{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.app.imports (etcd://...)",
  "description": "Etcd KV remote configuration provider. Configured via spring.app.imports URL.",
  "type": "object",
  "properties": {
    "etcd-url": {
      "type": "string",
      "description": "URL format: etcd://host:port/path/to/key"
    }
  }
}