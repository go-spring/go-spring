# Import configuration from an etcd cluster. "optional:" lets the app start
# even when the key does not exist yet; the value is filled in once published
# and refreshed live via the provider's change watcher.
spring.app.imports=optional:etcd:127.0.0.1:2379/gs-config-demo?format=properties
