{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.scheduler",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "description": "Enable the scheduler starter; active once at least one *scheduling.Job bean is present",
      "default": true
    }
  }
}
