spring.go-redis.instances.cache.addr=127.0.0.1:6379

spring.go-redis.instances.session.addr=127.0.0.1:6379

# This instance resolves its address via service discovery. `addr` is a dummy
# on purpose — it is ignored because `service-name` is set, proving the address
# comes from the registered discovery backend (see discovery.go), not config.
spring.go-redis.instances.discovery.addr=0.0.0.0:0
spring.go-redis.instances.discovery.service-name=redis-cluster

# Sentinel topology: connect to the master group "mymaster" resolved through the
# listed sentinel nodes. The bean type stays *redis.Client. Service discovery is
# not used here (sentinels do the node discovery); setting service-name in this
# mode is rejected at startup.
spring.go-redis.instances.sentinel.mode=sentinel
spring.go-redis.instances.sentinel.master-name=mymaster
spring.go-redis.instances.sentinel.sentinel-addrs=127.0.0.1:26379

# Cluster topology: seed the client with the cluster entry nodes. The bean type
# is *redis.ClusterClient (distinct from *redis.Client). `addrs` are seeds only;
# the client learns the full topology from the cluster. RouteByLatency/
# RouteRandomly and max-redirects are optional cluster tunables.
spring.go-redis.instances.cluster.mode=cluster
spring.go-redis.instances.cluster.addrs=127.0.0.1:7000,127.0.0.1:7001,127.0.0.1:7002,127.0.0.1:7003,127.0.0.1:7004,127.0.0.1:7005
spring.go-redis.instances.cluster.route-by-latency=true

# TLS is off by default. To dial Redis over TLS, enable it and point at the
# CA/cert files (client cert/key only needed for mutual TLS):
#   spring.go-redis.instances.cache.tls.enabled=true
#   spring.go-redis.instances.cache.tls.ca-file=/path/to/ca.pem
#   spring.go-redis.instances.cache.tls.cert-file=/path/to/client.pem
#   spring.go-redis.instances.cache.tls.key-file=/path/to/client.key
#   spring.go-redis.instances.cache.tls.server-name=redis.example.com
# The discovery backend bean name (a named discovery.Discovery bean
# registered in init) that resolves this instance's service-name.
spring.go-redis.instances.discovery.discovery=default
