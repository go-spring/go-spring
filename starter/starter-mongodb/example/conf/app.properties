spring.mongodb.instances.a.uri=mongodb://127.0.0.1:27017
spring.mongodb.instances.a.max-pool-size=100
spring.mongodb.instances.a.min-pool-size=1
spring.mongodb.instances.a.max-conn-idle-time=5m
spring.mongodb.instances.a.server-selection-timeout=10s

spring.mongodb.instances.b.uri=mongodb://127.0.0.1:27017
# TLS and authentication are disabled here because the smoke-test server is a
# plain local instance; the fields below document how to enable them.
# spring.mongodb.instances.b.username=root
# spring.mongodb.instances.b.password=secret
# spring.mongodb.instances.b.auth-source=admin
# spring.mongodb.instances.b.auth-mechanism=SCRAM-SHA-256
# spring.mongodb.instances.b.tls.enabled=true
# spring.mongodb.instances.b.tls.ca-cert-file=/etc/mongo/ca.pem
# spring.mongodb.instances.b.tls.cert-file=/etc/mongo/client.pem
# spring.mongodb.instances.b.tls.key-file=/etc/mongo/client-key.pem

# This instance resolves its address via service discovery. The uri host is a
# non-resolvable dummy on purpose — it is ignored because `service-name` is set,
# so a successful connection proves the address comes from the registered
# discovery backend (see discovery.go), not config. directConnection keeps the
# driver on the dialed seed instead of doing its own replica-set topology
# discovery.
spring.mongodb.instances.disc.uri=mongodb://nonexistent.invalid:27017/?directConnection=true
spring.mongodb.instances.disc.service-name=mongo-cluster
spring.mongodb.instances.disc.server-selection-timeout=10s
