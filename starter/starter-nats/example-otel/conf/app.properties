spring.nats.instances.main.url=nats://127.0.0.1:4222
spring.nats.instances.main.jetstream.enabled=true

spring.nats.instances.work.url=nats://127.0.0.1:4222

# TLS and authentication (optional). The local smoke broker uses neither, so
# these stay disabled here; a hardened deployment would set them, e.g.:
#   spring.nats.instances.main.tls.enabled=true
#   spring.nats.instances.main.tls.ca-file=/etc/nats/ca.pem
#   spring.nats.instances.main.tls.cert-file=/etc/nats/client-cert.pem
#   spring.nats.instances.main.tls.key-file=/etc/nats/client-key.pem
#   spring.nats.instances.main.creds-file=/etc/nats/app.creds
#   spring.nats.instances.main.nkey-file=/etc/nats/app.nk

# Observability: OTLP gRPC trace exporter -> Jaeger on :4317.
spring.observability.enable=true
spring.observability.service-name=nats-otel-example
spring.observability.trace.exporter=otlp-grpc
spring.observability.trace.endpoint=127.0.0.1:4317
spring.observability.trace.insecure=true
spring.observability.trace.sampler-ratio=1.0

spring.observability.metrics.exporter=prometheus
spring.observability.metrics.port=9090
spring.observability.metrics.path=/metrics

# The framework's SimpleHttpServer defaults to :9090 (spring.http.server.addr),
# which would collide with the prometheus scrape server above. This example
# serves no HTTP routes, so turn it off.
spring.http.server.enabled=false
