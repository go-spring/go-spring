{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "spring.app.imports (k8s://...)",
  "description": "Kubernetes ConfigMap/Secret configuration provider. Configured via spring.app.imports URL.",
  "type": "object",
  "properties": {
    "k8s-url": {
      "type": "string",
      "description": "URL format: k8s://namespace/configmap-name or k8s://namespace/secret-name"
    }
  }
}