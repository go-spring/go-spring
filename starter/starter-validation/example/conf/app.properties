# This example is a self-contained smoke test for go-spring validation and i18n.
# It exercises config-binding validation, web-path validation, and i18n message
# rendering purely via API calls — no Go-Spring container is required,
# so no spring.* properties are needed.

