import{_ as t,o,c as i,a0 as n}from"./chunks/framework.Dx25mwie.js";const g=JSON.parse('{"title":"/gs Hello, World!","description":"","frontmatter":{"head":[]},"headers":[],"relativePath":"en/docs/0.overview/ai-engineering.md","filePath":"en/docs/0.overview/ai-engineering.md"}'),a={name:"en/docs/0.overview/ai-engineering.md"};function s(r,e,l,d,h,c){return o(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="gs-hello-world" tabindex="-1">/gs Hello, World! <a class="header-anchor" href="#gs-hello-world" aria-label="Permalink to &quot;/gs Hello, World!&quot;">​</a></h1><p>In day-to-day Go-Spring work, <code>gs</code> is a command-line tool. Think of it as a small crew that helps you get things done around a project: <code>gs init</code> bootstraps a new one, <code>gs-http-gen</code> turns an IDL into HTTP server and client code, <code>gs-mock</code> scaffolds a bit of test plumbing. If all you need is a service that follows Go-Spring&#39;s conventions, these commands are usually enough.</p><p>Framed narrowly, <code>gs</code> solves the &quot;cold start&quot; problem — how to lay out the directory, generate the code, and cut down on boilerplate. Once the project is up, it quietly steps back and the team takes over.</p><p>Now the AI era is here, and <code>gs</code> needs an upgrade to keep up. After the upgrade, it becomes <code>/gs</code>.</p><p>The extra slash is the call sigil inside an AI conversation. <code>/gs</code> is not a new CLI — it&#39;s an AI Skill built around Go-Spring projects. It calls the underlying <code>gs</code> tools, but it also reads project context, understands Go-Spring conventions, runs builds and tests, writes docs, and organizes changes. A rough way to tell them apart: <code>gs</code> is the hammer, <code>/gs</code> is the person who knows when to swing it and cleans up afterwards.</p><p>From here, let&#39;s look at how far that shift can carry the development process.</p><h2 id="from-starting-a-project-to-living-with-a-project" tabindex="-1">From &quot;starting a project&quot; to &quot;living with a project&quot; <a class="header-anchor" href="#from-starting-a-project-to-living-with-a-project" aria-label="Permalink to &quot;From &quot;starting a project&quot; to &quot;living with a project&quot;&quot;">​</a></h2><p>A traditional scaffold stands at day zero of a project. It lays out the directories, installs the dependencies, and gets an example running — then its job is done. Whatever happens after that isn&#39;t really its concern.</p><p>The problem is, real engineering is never a one-shot thing. Once a Go-Spring project is running, it still has to survive changing requirements, new endpoints, config tweaks, component onboarding, missing tests, broken builds, production incidents, and doc drift. Most of a team&#39;s time is spent in this &quot;afterwards.&quot; What the project eventually looks like depends on whether each change respects the same engineering standard — not on how pretty the initial skeleton was.</p><p>What <code>/gs</code> wants to take on is precisely that &quot;afterwards&quot;:</p><ul><li><strong>Requirements</strong> — take a vague natural-language description and break it into concrete engineering tasks</li><li><strong>Design</strong> — reason about module boundaries, dependencies, and how Go-Spring components should be arranged</li><li><strong>Coding</strong> — make changes that follow the project&#39;s existing style, instead of dropping in an isolated snippet</li><li><strong>Testing</strong> — fill in the unit tests, integration tests, or examples along the way</li><li><strong>Building</strong> — know which module to enter so <code>go test</code> and <code>go build</code> land in the right place</li><li><strong>Delivery</strong> — put together the change summary, the risks, and the verification results</li><li><strong>Retention</strong> — write the lessons learned and conventions back into docs and Skills</li></ul><p>When the same entry point strings all of this together, <code>/gs</code> stops being merely a &quot;help me write some code&quot; assistant. It becomes more like &quot;help me get this done the Go-Spring way.&quot;</p><h2 id="a-middleware-sitting-inside-the-development-flow" tabindex="-1">A middleware sitting inside the development flow <a class="header-anchor" href="#a-middleware-sitting-inside-the-development-flow" aria-label="Permalink to &quot;A middleware sitting inside the development flow&quot;">​</a></h2><p>Anyone familiar with Go-Spring knows the word &quot;middleware&quot;: the framework lets you hang cross-cutting concerns — logging, auth, tracing — onto the request path or the application lifecycle. <code>/gs</code> does something similar, only inside the development flow.</p><p>It doesn&#39;t make decisions for you, but it steps in at a few key moments to make sure the standard motions actually happen:</p><ul><li>When a requirement arrives, it presses for clarity — goals, boundaries, and acceptance criteria</li><li>When a design takes shape, it pulls any implementation that drifts from the project structure back into the Go-Spring component model</li><li>When code is being changed, it holds the line on existing directory layout, naming, error handling, and configuration conventions</li><li>When tests run, it turns &quot;changed and shipped&quot; into &quot;changed and verified&quot;</li><li>When a build fails, it doesn&#39;t just hand back the red text — it points at where the problem is and suggests a way through</li><li>When something is being submitted, it fills in the change summary, verification status, and remaining risk</li></ul><p>The point of all this: <code>/gs</code> isn&#39;t a smarter code generator. It&#39;s more like adding a few checkpoints to the flow so that conventions move from &quot;written in the docs&quot; to &quot;walked through every time.&quot;</p><h2 id="what-does-engineering-discipline-mean-in-the-ai-era" tabindex="-1">What does &quot;engineering discipline&quot; mean in the AI era <a class="header-anchor" href="#what-does-engineering-discipline-mean-in-the-ai-era" aria-label="Permalink to &quot;What does &quot;engineering discipline&quot; mean in the AI era&quot;">​</a></h2><p>When people talk about the AI era, the first instinct is often to let the model generate freely. But for a team doing real engineering, the more valuable direction is the opposite — draw a clear boundary and let the model work inside it.</p><p>For Go-Spring projects, the boundary is mostly common sense:</p><ul><li>The repository root is not necessarily a Go module — builds have to go into the specific subproject</li><li>Configuration, logging, IoC, and lifecycle are managed by the framework</li><li>Prefer an existing Starter when integrating a component</li><li>Errors need enough context when they&#39;re propagated upward</li><li>Dependency injection happens at startup; no runtime dynamic injection</li><li>Beans of the same name and type should not rely on implicit overrides — pick one explicitly through mutually exclusive conditions</li><li>Tests, examples, and docs evolve with the code, not as an afterthought</li></ul><p>If these conventions live only in people&#39;s heads, they warp as the project grows. What <code>/gs</code> does is translate them into context an AI can read and act on.</p><p>A few everyday scenarios make it plainer:</p><ul><li>Someone says &quot;add an endpoint&quot; — the right response isn&#39;t just spitting out a handler. It&#39;s asking which module it belongs to, whether the IDL needs to change, whether new config is required, how dependencies are injected, how errors are wrapped, how tests should cover it, and whether docs need an update.</li><li>Someone says &quot;just build it&quot; — you can&#39;t assume there&#39;s a <code>go.mod</code> at the root. Go-Spring repos are usually stitched together from multiple modules, so the right directory has to be picked first.</li><li>Someone says &quot;wire up Redis&quot; — no need to write initialization from scratch. Check whether a Starter already exists; if so, reuse it and let Go-Spring handle the config binding, bean registration, and lifecycle.</li></ul><p>That&#39;s roughly what engineering discipline looks like in the AI era: context-driven, convention-executed, and every change converges to a verifiable result.</p><h2 id="how-gs-gets-grounded" tabindex="-1">How <code>/gs</code> gets grounded <a class="header-anchor" href="#how-gs-gets-grounded" aria-label="Permalink to &quot;How `/gs` gets grounded&quot;">​</a></h2><p>Roughly three layers.</p><h3 id="_1-make-the-project-context-explicit" tabindex="-1">1. Make the project context explicit <a class="header-anchor" href="#_1-make-the-project-context-explicit" aria-label="Permalink to &quot;1. Make the project context explicit&quot;">​</a></h3><p>For an AI to participate reliably, the first thing it needs is to actually understand the project.</p><p>A Go-Spring project can put the key information in plain sight through <code>AGENTS.md</code>, coding style guides, directory conventions, sample projects, and documentation:</p><ul><li>How the project is organized</li><li>Which directories are independent Go modules</li><li>The common build, test, and run commands</li><li>What is expected around error handling and logging</li><li>Which examples to imitate when adding a new feature</li><li>Which changes require a doc update</li></ul><p>The clearer this is, the more consistently <code>/gs</code> behaves. The team doesn&#39;t have to re-explain the rules every time, and the AI doesn&#39;t have to guess.</p><h3 id="_2-turn-common-actions-into-flows" tabindex="-1">2. Turn common actions into flows <a class="header-anchor" href="#_2-turn-common-actions-into-flows" aria-label="Permalink to &quot;2. Turn common actions into flows&quot;">​</a></h3><p>The second step is to lock down high-frequency actions as flows.</p><p>&quot;Adding an HTTP endpoint&quot; sounds like writing a few lines of code, but unpacked it&#39;s a sequence with a clear order:</p><ol><li>Pin down the semantics, inputs, and outputs</li><li>Decide whether the IDL needs a change and code needs to be regenerated</li><li>Find the target module and its existing layering</li><li>Modify the handler, service, config, and registration</li><li>Add tests or examples</li><li>Run gofmt, tests, and the build</li><li>Summarize the changes and verification</li></ol><p><code>/gs</code> can run this sequence by default. The user states the goal, the AI works through the middle, and where a real choice has to be made, it explains what was picked and why.</p><h3 id="_3-push-capability-into-tools" tabindex="-1">3. Push capability into tools <a class="header-anchor" href="#_3-push-capability-into-tools" aria-label="Permalink to &quot;3. Push capability into tools&quot;">​</a></h3><p>The third step is for <code>/gs</code> to lean on tools instead of stuffing logic into prompts.</p><p>A Go-Spring project already has a decent toolkit sitting right there:</p><ul><li><code>gs init</code> — scaffold a project that respects conventions</li><li><code>gs-http-gen</code> — generate HTTP code from an IDL</li><li><code>gs-mock</code> — generate test helper code</li><li><code>go test</code> — verify module behavior</li><li><code>go build</code> — verify the artifact builds</li><li><code>gofmt</code> — keep formatting consistent</li><li>Documentation build tools — confirm the site still renders</li></ul><p>The value of an AI Skill is knowing which tool to invoke when, and being able to read what the tool&#39;s output actually means for the project. It isn&#39;t a natural-language wrapper on top of a CLI — it&#39;s a CLI plugged into a full development flow.</p><h2 id="what-a-gs-request-looks-like-end-to-end" tabindex="-1">What a <code>/gs</code> request looks like end to end <a class="header-anchor" href="#what-a-gs-request-looks-like-end-to-end" aria-label="Permalink to &quot;What a `/gs` request looks like end to end&quot;">​</a></h2><p>In the ideal case, the user&#39;s input can be as short as this:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/gs Add a &quot;get user detail&quot; HTTP endpoint to the user service, keyed by user_id, and update the tests and docs while you&#39;re at it.</span></span></code></pre></div><p>What <code>/gs</code> needs to do goes well beyond &quot;write an endpoint&quot;:</p><ul><li>Read the project structure and locate the module for the user service</li><li>Figure out how the existing IDL, routing, handler, service, and repository are organized</li><li>Generate or modify code so naming and layering match the current style</li><li>Use Go-Spring&#39;s IoC, config, and lifecycle to organize dependencies</li><li>Wrap errors the way this project does, keeping enough context to trace them</li><li>Add tests covering the happy path and a few key failure paths</li><li>Run gofmt, go test, and go build when needed</li><li>Update the docs or examples to describe the endpoint&#39;s behavior</li><li>Wrap up with a change summary, verification commands, and remaining risks</li></ul><p>Underneath, this is a different way of working: instead of switching tools by hand one at a time, you hand the goal to <code>/gs</code> and let it break the goal into a series of standard motions.</p><h2 id="what-this-means-for-go-spring" tabindex="-1">What this means for Go-Spring <a class="header-anchor" href="#what-this-means-for-go-spring" aria-label="Permalink to &quot;What this means for Go-Spring&quot;">​</a></h2><p>Go-Spring has always been about engineering discipline on the application side — configuration, dependency injection, logging, lifecycle, component integration. All of it together is meant to keep Go services simple while giving them a steadier foundation.</p><p><code>/gs</code> pushes that discipline one step earlier, into the development process itself.</p><p>The framework handles how the application organizes itself at runtime; <code>/gs</code> handles how development organizes itself. One makes startup, wiring, and shutdown more predictable; the other makes requirements, code, tests, and delivery more predictable. Put them together, and Go-Spring&#39;s engineering discipline lives not only in the code but in how the team works day to day:</p><ul><li>A unified lifecycle at runtime</li><li>A single entry point during development</li><li>A standard way to onboard components</li><li>A reusable flow for turning requirements into code</li><li>Default verification steps for every change</li><li>A stable place for hard-won lessons to accumulate</li></ul><h2 id="after-hello-world" tabindex="-1">After &quot;Hello, World&quot; <a class="header-anchor" href="#after-hello-world" aria-label="Permalink to &quot;After &quot;Hello, World&quot;&quot;">​</a></h2><p>&quot;Hello, World&quot; is usually a technology&#39;s first greeting to the outside world.</p><p><code>/gs Hello, World!</code> isn&#39;t a minimal example — it&#39;s a new entry point. From here on, requirements in a Go-Spring project can be understood, code can be changed, tests can be run, builds can be verified, docs can be kept up to date, and lessons can actually stick.</p><p><code>gs</code> makes starting a project easier. <code>/gs</code> makes living with one easier.</p><p>What matters in the AI era isn&#39;t the occasional working snippet an AI produces — it&#39;s whether AI can consistently participate in a development flow that is reusable, verifiable, and worth keeping around. For Go-Spring, <code>/gs</code> is the entry point to that flow.</p>',58)])])}const u=t(a,[["render",s]]);export{g as __pageData,u as default};
