import{_ as i,o as n,c as t,a0 as o}from"./chunks/framework.Dx25mwie.js";const m=JSON.parse('{"title":"Project Overview","description":"","frontmatter":{"head":[]},"headers":[],"relativePath":"en/docs/0.overview/overview.md","filePath":"en/docs/0.overview/overview.md"}'),a={name:"en/docs/0.overview/overview.md"};function r(s,e,l,c,d,p){return n(),t("div",null,[...e[0]||(e[0]=[o('<h1 id="project-overview" tabindex="-1">Project Overview <a class="header-anchor" href="#project-overview" aria-label="Permalink to &quot;Project Overview&quot;">​</a></h1><p>Go-Spring is a <strong>lightweight application development framework</strong> for Go. It draws on excellent design ideas proven over many years in the Java Spring framework, while staying true to Go&#39;s native simplicity and efficiency. It avoids overengineering and unnecessary complexity, aiming to balance engineering capability with developer experience.</p><h2 id="why-build-go-spring" tabindex="-1">Why Build Go-Spring? <a class="header-anchor" href="#why-build-go-spring" aria-label="Permalink to &quot;Why Build Go-Spring?&quot;">​</a></h2><p>Since its creation, Go has quickly gained a strong foothold in server-side development thanks to its simple syntax, high performance, and excellent concurrency model. However, the community still lacks a widely recognized, unified approach for elegantly building server-side applications.</p><h3 id="pain-points-of-native-development" tabindex="-1">Pain Points of Native Development <a class="header-anchor" href="#pain-points-of-native-development" aria-label="Permalink to &quot;Pain Points of Native Development&quot;">​</a></h3><p>When building server-side applications from scratch with native Go, developers often encounter a set of common problems:</p><ul><li><p><strong>Scattered configuration management</strong></p><p>Configuration loading, struct binding, and parameter validation often need to be implemented manually. Projects repeatedly reinvent the wheel and lack unified conventions.</p></li><li><p><strong>Cumbersome dependency management</strong></p><p>Component dependencies are organized through manual instantiation, which creates tight coupling and makes testing, extension, and replacement harder.</p></li><li><p><strong>No unified lifecycle management</strong></p><p>Each component controls its own startup and shutdown process. Without coordination, graceful shutdown is difficult to implement correctly.</p></li><li><p><strong>Repeated work integrating basic components</strong></p><p>Each time components such as MySQL or Redis are introduced, developers need to rewrite boilerplate such as connection initialization and configuration parsing, which is inefficient.</p></li></ul><p>These problems are not defects in Go itself. They arise from the lack of a unified foundation framework that provides standardized solutions.</p><h3 id="trade-offs-in-existing-solutions" tabindex="-1">Trade-offs in Existing Solutions <a class="header-anchor" href="#trade-offs-in-existing-solutions" aria-label="Permalink to &quot;Trade-offs in Existing Solutions&quot;">​</a></h3><p>The Go community already has several dependency injection frameworks, but each focuses on different concerns and is difficult to use as a complete solution:</p><ul><li><p><strong>wire</strong></p><p>Implements dependency injection through compile-time code generation. It uses no reflection and has excellent performance, but the workflow is relatively cumbersome, and code must be regenerated after every change.</p></li><li><p><strong>dig/fx</strong></p><p>Provides constructor-based dependency injection and powerful capabilities, but relies on runtime resolution. Its overall design is relatively complex and has a higher learning curve.</p></li></ul><p>Fundamentally, these frameworks <strong>mainly focus on dependency injection itself</strong>. They provide limited support for infrastructure such as configuration management, logging, lifecycle control, and component integration, so developers still need to assemble those pieces themselves.</p><p>Meanwhile, Web frameworks such as gin, echo, and fiber focus on HTTP routing and do not take responsibility for the application infrastructure layer.</p><h3 id="go-spring-s-vision" tabindex="-1">Go-Spring&#39;s Vision <a class="header-anchor" href="#go-spring-s-vision" aria-label="Permalink to &quot;Go-Spring&#39;s Vision&quot;">​</a></h3><p>Go-Spring&#39;s vision is to build a <strong>complete, one-stop application development framework</strong> that solves these core problems in a unified way:</p><ul><li>Configuration management</li><li>Dependency injection</li><li>Lifecycle control</li><li>Basic component integration</li></ul><p>By providing a systematic solution, Go-Spring helps developers break free from repetitive infrastructure work, focus more on business logic, and build applications efficiently and elegantly.</p><h3 id="go-spring-s-positioning" tabindex="-1">Go-Spring&#39;s Positioning <a class="header-anchor" href="#go-spring-s-positioning" aria-label="Permalink to &quot;Go-Spring&#39;s Positioning&quot;">​</a></h3><p>From a core design perspective, Go-Spring is built on four foundational capabilities: <strong>configuration management, logging, dependency injection, and lifecycle management</strong>. On top of these, it extends into a complete ecosystem that can support HTTP, gRPC, and other application scenarios.</p><p>Therefore, Go-Spring&#39;s positioning can be understood from two perspectives: &quot;cooperation&quot; and &quot;competition&quot;.</p><h4 id="cooperation-coexisting-with-domain-frameworks" tabindex="-1">Cooperation: Coexisting with Domain Frameworks <a class="header-anchor" href="#cooperation-coexisting-with-domain-frameworks" aria-label="Permalink to &quot;Cooperation: Coexisting with Domain Frameworks&quot;">​</a></h4><ul><li><p><strong>Does not replace mainstream Web frameworks</strong></p><p>Go-Spring does not replace frameworks such as gin, echo, or fiber that focus on HTTP routing. You can still use the tools you are familiar with, while Go-Spring handles configuration, dependencies, and lifecycle management in a unified way.</p></li><li><p><strong>Keeps a simple Go-style design</strong></p><p>Go-Spring does not copy Java Spring&#39;s heavyweight system. Instead, it borrows its core ideas while preserving Go&#39;s usual lightweight and clear style.</p></li><li><p><strong>Uses a &quot;startup-time IoC&quot; mechanism</strong></p><p>All dependencies are injected during application startup. Runtime dynamic injection is not supported. This design significantly reduces complexity while avoiding runtime performance overhead.</p></li></ul><h4 id="competition-competing-with-complete-application-frameworks" tabindex="-1">Competition: Competing with Complete Application Frameworks <a class="header-anchor" href="#competition-competing-with-complete-application-frameworks" aria-label="Permalink to &quot;Competition: Competing with Complete Application Frameworks&quot;">​</a></h4><ul><li><p><strong>Direct competition through one-stop capabilities</strong></p><p>When you need a complete solution covering configuration, dependency injection, logging, and component integration, Go-Spring naturally competes with full-stack microservice frameworks such as go-zero, kitex, Kratos, and go-frame.</p></li><li><p><strong>Core capabilities can be split out and reused</strong></p><p>Even in competitive scenarios, Go-Spring&#39;s core modules (configuration, dependency injection, logging) remain highly independent and can be integrated into other frameworks to complement and coexist with them.</p></li></ul><p>Go-Spring always maintains an open mindset. In the future, it will actively collaborate with the community, integrate more excellent projects, and gradually build a more complete ecosystem.</p><h2 id="go-spring-s-design-goals" tabindex="-1">Go-Spring&#39;s Design Goals <a class="header-anchor" href="#go-spring-s-design-goals" aria-label="Permalink to &quot;Go-Spring&#39;s Design Goals&quot;">​</a></h2><p>Go-Spring&#39;s design is centered on eight core principles, aiming to balance &quot;ease of use&quot; with &quot;flexibility&quot;.</p><h3 id="convention-over-configuration" tabindex="-1">Convention over Configuration <a class="header-anchor" href="#convention-over-configuration" aria-label="Permalink to &quot;Convention over Configuration&quot;">​</a></h3><p>Go-Spring provides a set of reasonable default conventions, allowing developers to start projects quickly without complex configuration in most scenarios. For example:</p><ul><li>Logs are output to the console by default, with the INFO level, ready to use out of the box</li><li>The HTTP service listens on <code>:8080</code> by default, while still supporting customization as needed</li><li>pprof listens on <code>:6060</code> by default and can be disabled through configuration</li></ul><p>All default behaviors can be flexibly adjusted through configuration. From log output methods to component startup and shutdown controls, everything is highly customizable.</p><p>In short:</p><ul><li><strong>Convention</strong> helps you get started quickly</li><li><strong>Configuration</strong> helps you handle complex scenarios flexibly</li></ul><h3 id="startup-time-dependency-injection" tabindex="-1">Startup-Time Dependency Injection <a class="header-anchor" href="#startup-time-dependency-injection" aria-label="Permalink to &quot;Startup-Time Dependency Injection&quot;">​</a></h3><p>This is one of the most important design decisions in the Go-Spring architecture: <strong>all dependencies are injected only during application startup, and Beans cannot be dynamically retrieved or injected at runtime</strong>.</p><p>This choice is mainly based on the following considerations:</p><ul><li><p><strong>Simplicity</strong></p><p>Dependency injection runs only once at startup. There is no need to maintain complex metadata structures at runtime, reducing system complexity and saving memory.</p></li><li><p><strong>Predictability</strong></p><p>All dependencies are resolved during startup, avoiding uncertain issues such as &quot;missing dependencies&quot; during runtime and making system behavior more stable and easier to reason about.</p></li><li><p><strong>High performance</strong></p><p>No reflection or dynamic dependency resolution is needed at runtime. All components are already assembled at startup and can be used directly, resulting in stable and efficient performance.</p></li><li><p><strong>Consistent with Go&#39;s engineering philosophy</strong></p><p>Go encourages discovering problems as early as possible at compile time or startup time and failing fast. This design closely matches Go developers&#39; habits.</p></li></ul><p>Of course, this choice also means giving up advanced features such as &quot;runtime dynamic injection&quot;. But in the vast majority of server-side application scenarios, these capabilities are not essential. Trading lower complexity for higher performance and maintainability is a more cost-effective compromise.</p><blockquote><p>Unlike Java Spring, Go-Spring <strong>does not require interfaces for dependency injection</strong>. In most scenarios, injecting concrete types directly is enough; there is no need to over-abstract purely for &quot;decoupling&quot;. Interfaces are recommended only when multiple interchangeable implementations are truly needed.</p></blockquote><h3 id="container-level-singletons" tabindex="-1">Container-Level Singletons <a class="header-anchor" href="#container-level-singletons" aria-label="Permalink to &quot;Container-Level Singletons&quot;">​</a></h3><p>All Beans in the container are singletons and unique for the lifetime of the container.</p><p><strong>Why design it this way?</strong></p><p>Because in most business scenarios, one component only needs one instance. For example, your <code>UserService</code> or <code>DataSource</code> only needs one instance for the whole application. There is no need to create a new one every time it is used.</p><p>If you truly need multiple different instances of the same type (for example, one primary data source and one replica data source), you can register them as different <strong>named Beans</strong> and select by name during injection. This covers most scenarios that require multiple instances.</p><h3 id="explicit-over-implicit" tabindex="-1">Explicit over Implicit <a class="header-anchor" href="#explicit-over-implicit" aria-label="Permalink to &quot;Explicit over Implicit&quot;">​</a></h3><p>Go-Spring insists on explicit declarations and avoids implicit automatic inference:</p><ul><li>Interface exports must be <strong>explicitly declared</strong> and are not inferred automatically</li><li>Dependencies are clearly visible; the code itself is the documentation</li></ul><p><strong>Why design it this way?</strong></p><p>Go itself is an explicit language. Although implicit automatic inference may save a few lines of code, it can easily produce surprises. For example, if a struct accidentally implements all methods of an interface and is automatically inferred and registered by the container, that is usually not what you want.</p><p>Explicit declarations make dependencies clear at a glance. You can see all exported relationships by reading the code, without extra explanations.</p><h3 id="conditional-mutual-exclusion-over-override" tabindex="-1">Conditional Mutual Exclusion over Override <a class="header-anchor" href="#conditional-mutual-exclusion-over-override" aria-label="Permalink to &quot;Conditional Mutual Exclusion over Override&quot;">​</a></h3><p>Beans with the same name and same type cannot override each other. Which Bean takes effect must be determined by conditional matching.</p><p>Java Spring allows multiple Beans with the same name and then selects the primary version through <code>@Primary</code>. Go-Spring considers this implicit selection error-prone, so it insists on conditional mutual exclusion.</p><p><strong>Why design it this way?</strong></p><p>If Bean overriding were allowed, it would be hard to tell which Bean overrides which, and debugging would be difficult when problems occur. Conditional matching makes it explicit when each Bean takes effect. If a conflict appears during startup, the framework reports an error directly and asks you to handle it. This is far better than silently overriding for you.</p><p>In practice, this design also feels natural: different environments (dev/prod) need different implementations. Register them with different conditions and use configuration switches to decide which one is enabled. This matches the simplest logical habit.</p><h3 id="go-semantics-first" tabindex="-1">Go Semantics First <a class="header-anchor" href="#go-semantics-first" aria-label="Permalink to &quot;Go Semantics First&quot;">​</a></h3><p>Go-Spring does not deliberately recreate Java Spring&#39;s object-oriented model. Instead, it always adheres to Go&#39;s native semantics and development habits:</p><ul><li>Error handling follows Go&#39;s <code>error</code> return mechanism rather than an exception system</li><li>Complex mechanisms such as AOP or dynamic proxies are avoided, keeping code transparent, readable, and debuggable</li><li>Interfaces are native Go interfaces, with no framework-specific markers or constraints</li></ul><p>Ultimately, Go-Spring&#39;s philosophy is: <strong>let Go remain Go</strong>. The framework only manages dependencies and lifecycle; it does not change your coding style or mental model.</p><h3 id="modularity-and-componentization" tabindex="-1">Modularity and Componentization <a class="header-anchor" href="#modularity-and-componentization" aria-label="Permalink to &quot;Modularity and Componentization&quot;">​</a></h3><p>Go-Spring encapsulates components through the Starter mechanism, achieving true <strong>on-demand introduction</strong>:</p><ul><li>Need MySQL? Introduce the corresponding MySQL Starter</li><li>Need Redis? Introduce the corresponding Redis Starter</li><li>Unused components are not compiled into the final artifact, avoiding unnecessary size growth</li></ul><p>Each Starter is an independent module responsible for its own configuration parsing and component registration. Developers do not need to write verbose initialization code. Integration can be completed with a <strong>blank import</strong>, greatly reducing onboarding cost.</p><h3 id="unified-lifecycle-management" tabindex="-1">Unified Lifecycle Management <a class="header-anchor" href="#unified-lifecycle-management" aria-label="Permalink to &quot;Unified Lifecycle Management&quot;">​</a></h3><p>Go-Spring uniformly schedules and manages the full application lifecycle from startup to shutdown:</p><ul><li><p><strong>Clear and orderly startup process</strong></p><p>Initialization proceeds step by step in the order of &quot;configuration → logging → IoC → Runner → Server&quot;, with clear layers.</p></li><li><p><strong>Graceful and reliable shutdown process</strong></p><p>Services (Server) are stopped first, then dependencies (IoC) are released, and finally logs are flushed to ensure resources are reclaimed correctly and logs are not lost.</p></li><li><p><strong>Unified interface conventions</strong></p><p>All components follow the unified <code>Run / Stop</code> interface and are orchestrated and scheduled by the framework.</p></li></ul><p>Developers no longer need to manually coordinate component startup and shutdown order. The framework handles these details for you, making applications more stable and controllable.</p><h2 id="go-spring-features" tabindex="-1">Go-Spring Features <a class="header-anchor" href="#go-spring-features" aria-label="Permalink to &quot;Go-Spring Features&quot;">​</a></h2><h3 id="powerful-configuration-management" tabindex="-1">Powerful Configuration Management <a class="header-anchor" href="#powerful-configuration-management" aria-label="Permalink to &quot;Powerful Configuration Management&quot;">​</a></h3><ul><li>Multi-format support: natively compatible with <code>properties</code>, <code>YAML</code>, <code>TOML</code>, and <code>JSON</code></li><li>Multi-source access: supports local files, environment variables, command-line arguments, and extensible integration with configuration centers such as K8s ConfigMap, etcd, Nacos, and ZooKeeper</li><li>Flexible variable mechanism: supports <code>${key}</code> and <code>${key:=default}</code>, making configuration reuse and default values easy</li><li>Direct struct binding: configuration can be bound directly to Go structs, with native support for nested structs, slices, and maps</li><li>Built-in type conversion: supports common types such as <code>time.Time</code> and <code>time.Duration</code> out of the box</li><li>Complete validation system: supports required fields, ranges, enums, regular expressions, and custom rules</li><li><strong>Dynamic hot update</strong>: configuration refreshes automatically at runtime without restarting the service</li></ul><h3 id="lightweight-and-efficient-ioc-container" tabindex="-1">Lightweight and Efficient IoC Container <a class="header-anchor" href="#lightweight-and-efficient-ioc-container" aria-label="Permalink to &quot;Lightweight and Efficient IoC Container&quot;">​</a></h3><ul><li>Completes all dependency injection at startup, with zero extra runtime overhead</li><li>Conditional assembly based on Profiles and Conditions, adapting flexibly to different environments</li><li>Explicit dependency declarations: rejects implicit inference and avoids &quot;magic behavior&quot;</li><li>On-demand instantiation: creates only Beans that are actually depended on, reducing resource consumption</li><li>No override design: forbids Bean overriding and uses conditions for clear mutual exclusion</li><li>Supports constructor injection plus struct field injection, balancing flexibility and readability</li></ul><h3 id="controllable-application-lifecycle" tabindex="-1">Controllable Application Lifecycle <a class="header-anchor" href="#controllable-application-lifecycle" aria-label="Permalink to &quot;Controllable Application Lifecycle&quot;">​</a></h3><ul><li>Linear startup mechanism: any step failure terminates startup immediately, avoiding a &quot;half-started&quot; state</li><li>Graceful shutdown: captures system signals and safely releases resources in dependency order</li><li>Global Context throughout: unified lifecycle management that prevents abuse of <code>context.Background</code></li><li>Dual startup modes: <ul><li><code>Run()</code>: standard mode, fully managed application lifecycle</li><li><code>RunAsync()</code>: compatibility mode, easily integrated into existing projects</li></ul></li></ul><h3 id="business-oriented-logging-system" tabindex="-1">Business-Oriented Logging System <a class="header-anchor" href="#business-oriented-logging-system" aria-label="Permalink to &quot;Business-Oriented Logging System&quot;">​</a></h3><ul><li>🌟 <strong>Tag routing (core innovation)</strong>: routes and splits logs based on business tags to precisely control log destinations</li><li>Fully structured logs: represented uniformly as Fields, naturally fitting log analysis and search systems</li><li>Pluggable outputs: supports Console, files, and time-rotated files with automatic cleanup</li><li>High-performance writing: supports synchronous/asynchronous modes; asynchronous writing does not block business logic</li><li>Multi-format support: built-in Text and JSON formats, with support for custom formats</li><li>Automatic trace injection: automatically extracts Trace information from <code>context.Context</code> to connect call chains</li></ul><h3 id="flexibly-integrated-http-server" tabindex="-1">Flexibly Integrated HTTP Server <a class="header-anchor" href="#flexibly-integrated-http-server" aria-label="Permalink to &quot;Flexibly Integrated HTTP Server&quot;">​</a></h3><ul><li>Fully compatible with Go&#39;s standard library <code>net/http</code>, seamlessly integrating with any third-party router framework</li><li>Lifecycle is uniformly managed by the framework, automatically handling startup and shutdown</li><li>Native graceful shutdown support ensures requests are processed safely before exit</li><li>Supports configuration of key parameters such as port and timeouts, and can also be completely disabled as needed</li></ul><h3 id="out-of-the-box-starter-mechanism" tabindex="-1">Out-of-the-Box Starter Mechanism <a class="header-anchor" href="#out-of-the-box-starter-mechanism" aria-label="Permalink to &quot;Out-of-the-Box Starter Mechanism&quot;">​</a></h3><ul><li>Modular component encapsulation, automatically registered through <code>blank import</code></li><li>Provides three registration methods: <ul><li><code>provide</code>: basic component registration</li><li><code>module</code>: modular encapsulation</li><li><code>group</code>: supports multi-instance scenarios such as multiple data sources or clients</li></ul></li><li>Configuration-driven enable/disable behavior, achieving true &quot;load on demand&quot;</li><li>Instantiates by dependency relationships; unused components are not created, avoiding resource waste</li></ul><h3 id="go-idiomatic-testing-support" tabindex="-1">Go-Idiomatic Testing Support <a class="header-anchor" href="#go-idiomatic-testing-support" aria-label="Permalink to &quot;Go-Idiomatic Testing Support&quot;">​</a></h3><ul><li>Fully compatible with native <code>go test</code>, with zero learning cost</li><li>Supports IoC test mode: <code>RunTest()</code> starts the application context with one call and automatically isolates test data</li><li>Built-in fluent assertion mechanism, supporting both <code>assert</code> and <code>require</code> styles</li><li>Integrated Mock capabilities: supports interface mocking and method-level monkey patching</li></ul><h3 id="contract-driven-http-code-generation" tabindex="-1">Contract-Driven HTTP Code Generation <a class="header-anchor" href="#contract-driven-http-code-generation" aria-label="Permalink to &quot;Contract-Driven HTTP Code Generation&quot;">​</a></h3><ul><li>Describes HTTP / RPC interfaces based on IDL (Interface Definition Language), providing unified contracts</li><li><strong>Define once, generate for both sides</strong>: automatically generates server skeleton code and client invocation code</li><li>Complete type system support: covers primitive types, lists/maps, enums, oneof (union types), and generic structs</li><li>Supports <code>required</code> / <code>optional</code> field modifiers to clearly express data constraints</li><li>Rich syntax capabilities: supports constants, custom annotation extensions, normal RPC, and SSE streaming interfaces</li><li>Automatically completes HTTP parameter binding: path parameters, query parameters, request headers, and request bodies are uniformly mapped</li><li>Built-in high-performance validation mechanism: generates validation logic based on expressions, with no reflection overhead</li></ul><h2 id="go-spring-vs-other-solutions" tabindex="-1">Go-Spring vs Other Solutions <a class="header-anchor" href="#go-spring-vs-other-solutions" aria-label="Permalink to &quot;Go-Spring vs Other Solutions&quot;">​</a></h2><h3 id="vs-pure-manual-wiring" tabindex="-1">vs Pure Manual Wiring <a class="header-anchor" href="#vs-pure-manual-wiring" aria-label="Permalink to &quot;vs Pure Manual Wiring&quot;">​</a></h3><p>Using purely manual dependency assembly is simple, direct, framework-free, and gives you full control over all logic. However, this approach is acceptable only when the project is small. As the business grows, problems gradually emerge: large amounts of repeated component creation and dependency assembly code, repeated handwritten configuration parsing, and no unified lifecycle management, eventually causing maintenance costs to rise quickly.</p><p><strong>Go-Spring&#39;s value lies in</strong>: encapsulating common foundational capabilities such as configuration loading, dependency management, and lifecycle control, so you can focus on business logic instead of reinventing the wheel.</p><h3 id="vs-wire" tabindex="-1">vs Wire <a class="header-anchor" href="#vs-wire" aria-label="Permalink to &quot;vs Wire&quot;">​</a></h3><p><a href="https://github.com/google/wire" target="_blank" rel="noreferrer">Wire</a> is a compile-time dependency injection tool from Google. It is based on compile-time code generation, requires no reflection, and has excellent runtime performance. Its shortcomings are: code must be regenerated for every dependency change, the workflow is relatively cumbersome, and it only solves dependency injection. Configuration, logging, lifecycle, and other concerns still need to be integrated manually, and support for multi-environment scenarios is weak.</p><p><strong>How Go-Spring differs</strong>:</p><ul><li>No code generation is required; changes to dependencies or configuration take effect after restarting, making the development experience smoother</li><li>Provides a one-stop solution for configuration, logging, dependency injection, and lifecycle</li><li>Natively supports conditional injection and multi-environment configuration out of the box</li></ul><h3 id="vs-dig-fx" tabindex="-1">vs dig/fx <a class="header-anchor" href="#vs-dig-fx" aria-label="Permalink to &quot;vs dig/fx&quot;">​</a></h3><p><a href="https://github.com/uber-go/dig" target="_blank" rel="noreferrer">dig</a>/<a href="https://github.com/uber-go/fx" target="_blank" rel="noreferrer">fx</a> are dependency injection frameworks from Uber. They are powerful, support runtime dynamic injection, have a relatively mature ecosystem, and are suitable for complex systems. However, their design is more complex, introducing many concepts (modules, Providers, Decorators, lifecycles, etc.). They have a higher learning curve, and because they support runtime injection, they need to retain complete dependency metadata, resulting in some runtime overhead.</p><p><strong>Go-Spring&#39;s trade-offs</strong>:</p><ul><li>Uses a &quot;startup-time one-shot injection&quot; strategy. All dependencies are resolved during startup, with no extra metadata needed at runtime, making it lighter overall</li><li>Concepts are streamlined, the learning curve is gentle, and daily usage only requires attention to core injection methods such as <code>@Autowired</code></li><li>The configuration system is deeply integrated and covers complete processes such as configuration binding and validation, which most DI frameworks do not solve systematically</li></ul><p>Overall, Go-Spring is not a single-point optimization of one capability such as DI. Instead, it starts from the <strong>overall application infrastructure perspective</strong> and provides a more complete, unified solution that balances development efficiency, maintainability, and complexity.</p><h3 id="vs-java-spring" tabindex="-1">vs Java Spring <a class="header-anchor" href="#vs-java-spring" aria-label="Permalink to &quot;vs Java Spring&quot;">​</a></h3><p>Many people see the name Go-Spring and mistakenly think it is a direct port of Java Spring, but the two are fundamentally different:</p><ul><li><p><strong>Java Spring</strong> has evolved for more than twenty years into an extremely complete enterprise framework, covering many advanced features such as runtime dynamic weaving, AOP, and automatic scanning. Its system is large and complex.</p></li><li><p><strong>Go-Spring</strong> focuses on &quot;taking the essence&quot;. It keeps only the core ideas proven effective in Spring&#39;s long-term practice (such as configuration management, dependency injection, and the Starter pattern), and reimplements them in a native Go way, avoiding complex mechanisms that do not fit Go style.</p></li></ul><p>Therefore, Go-Spring is not a simple copy of Java Spring. It is a <strong>concept-based reconstruction</strong>: inheriting the ideas while adhering to Go&#39;s simplicity and clarity.</p><h3 id="vs-full-stack-microservice-frameworks" tabindex="-1">vs Full-Stack Microservice Frameworks <a class="header-anchor" href="#vs-full-stack-microservice-frameworks" aria-label="Permalink to &quot;vs Full-Stack Microservice Frameworks&quot;">​</a></h3><p>go-zero/kitex/Kratos/go-frame are mature full-stack microservice solutions in the Go ecosystem that have been validated in large-scale production. They have complete microservice ecosystems with built-in service discovery, circuit breaking and degradation, rate limiting, tracing, observability, code generation, and more. They are basically ready to use out of the box, have abundant community resources, and make it easy to build systems from scratch.</p><h4 id="go-spring-s-current-state" tabindex="-1">Go-Spring&#39;s Current State <a class="header-anchor" href="#go-spring-s-current-state" aria-label="Permalink to &quot;Go-Spring&#39;s Current State&quot;">​</a></h4><p>As a later project, Go-Spring still has gaps in the completeness and richness of its microservice ecosystem, and official components are still being continuously improved. The current core capabilities (configuration, dependency injection, logging, and lifecycle management) are relatively stable, but the surrounding ecosystem still needs time to mature.</p><h4 id="go-spring-s-advantages" tabindex="-1">Go-Spring&#39;s Advantages <a class="header-anchor" href="#go-spring-s-advantages" aria-label="Permalink to &quot;Go-Spring&#39;s Advantages&quot;">​</a></h4><ul><li><p><strong>A simpler core design</strong></p><ul><li>The configuration system supports multiple formats and sources, with default values, environment variable overrides, dynamic refresh, and other capabilities while keeping the design clear</li><li>Dependency injection supports both constructor injection and field injection, with intuitive conditional injection logic and no implicit behavior</li></ul></li><li><p><strong>Higher modularity</strong></p><ul><li>Use only the configuration module ✔</li><li>Use only the DI module ✔</li><li>Combine with any Web framework ✔</li><li>Use the complete framework ✔</li></ul><p>Everything is selected on demand, rather than being forcibly bound together.</p></li><li><p><strong>A design philosophy that better fits Go</strong></p><p>It emphasizes simplicity, explicitness, and low magic, avoiding overengineering and making code easier to understand and maintain.</p></li></ul><p>The most important point is: even if you have already chosen one of these mature microservice frameworks, Go-Spring&#39;s core capabilities (configuration, DI, logging) can still be <strong>extracted independently and integrated into them</strong> to enhance an existing system rather than replace it.</p><p>This makes Go-Spring not only an &quot;alternative option&quot;, but also a set of <strong>composable foundational capability components</strong> that can truly cooperate and coexist with the existing ecosystem.</p><h2 id="go-spring-s-design-philosophy" tabindex="-1">Go-Spring&#39;s Design Philosophy <a class="header-anchor" href="#go-spring-s-design-philosophy" aria-label="Permalink to &quot;Go-Spring&#39;s Design Philosophy&quot;">​</a></h2><h3 id="simplicity-first" tabindex="-1">Simplicity First <a class="header-anchor" href="#simplicity-first" aria-label="Permalink to &quot;Simplicity First&quot;">​</a></h3><p>Go-Spring always follows the design principle of &quot;<strong>simplicity first</strong>&quot;:</p><ul><li>Work that can be completed at startup should not be moved to runtime</li><li>Logic that can be expressed explicitly should not rely on implicit &quot;magic&quot;</li><li>Avoid reflection as much as possible; when it is necessary, strictly limit it to the startup phase</li><li>Reject overengineering and do not introduce unnecessary complex features</li></ul><p>In short: <strong>solve problems in the most direct way, not in the flashiest way</strong>.</p><h3 id="modular-design" tabindex="-1">Modular Design <a class="header-anchor" href="#modular-design" aria-label="Permalink to &quot;Modular Design&quot;">​</a></h3><p>Go-Spring uses a highly modular architecture. Functional modules are decoupled from each other and can be used on demand:</p><ul><li>If you do not need HTTP capabilities, you can completely avoid introducing related modules</li><li>If you do not use http-gen, it will have no impact on your project</li><li>The framework does not force any technology choices</li></ul><p>This design lets you freely combine capabilities instead of being &quot;bundled&quot; by the framework.</p><h3 id="progressive-adoption" tabindex="-1">Progressive Adoption <a class="header-anchor" href="#progressive-adoption" aria-label="Permalink to &quot;Progressive Adoption&quot;">​</a></h3><p>Go-Spring supports multiple adoption modes, fitting projects at different stages and with different needs:</p><ul><li><strong>Full adoption</strong>: use the framework&#39;s capabilities across configuration, dependencies, and lifecycle</li><li><strong>Partial usage</strong>: use only the configuration module while keeping the rest of the original implementation</li><li><strong>Capability splitting</strong>: use only dependency injection (DI), while choosing any Web framework freely</li></ul><p>The framework does not force a unified solution. Instead, it provides flexible capability assembly: <strong>use as much as you need</strong>.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>Go-Spring tries to solve the repeated, tedious, and inefficient foundational problems in server-side development:</p><ul><li><p><strong>Unified configuration management</strong></p><p>No need to reimplement configuration loading logic in every project; the framework has built-in support for configuration binding, validation, and dynamic refresh.</p></li><li><p><strong>Clear dependency injection mechanism</strong></p><p>Dependency injection decouples components, making code structure clearer and making unit testing and mocking easier.</p></li><li><p><strong>Consistent lifecycle management</strong></p><p>The framework uniformly schedules component startup and shutdown processes, requiring no manual orchestration and ensuring graceful shutdown without losing requests.</p></li><li><p><strong>Out-of-the-box official components</strong></p><p>Common components such as MySQL, Redis, and pprof provide official Starters and can be used by importing them and adding a small amount of configuration.</p></li><li><p><strong>Unified development experience</strong></p><p>Teams share consistent project structure and configuration conventions, making onboarding easier for new members and code easier to maintain.</p></li></ul><p>It should be clear that Go-Spring is still growing: it is not yet a fully complete &quot;all-in-one&quot; framework, and it does not try to make all decisions for you. As a relatively young project, its microservice ecosystem still has room to improve, and it still needs time to mature compared with established frameworks.</p><p>But Go-Spring always insists on:</p><ul><li>Maintaining Go-style simplicity and clarity</li><li>Providing a set of cooperative and composable core modules</li><li>Helping developers build server-side applications efficiently</li></ul><p>We maintain an open attitude and encourage coexistence and collaboration with other frameworks. In the future, we will continue to work with the community, integrate more excellent projects, and gradually build a more complete and powerful ecosystem.</p>',130)])])}const g=i(a,[["render",r]]);export{m as __pageData,g as default};
