# Consumer (client-role) configuration for the etcd registry example.

# The built-in HTTP server is disabled so the consumer runs server-less.
spring.http.server.enabled=false

# Application identity: required by starter-dubbo.
spring.dubbo.application.name=greet-zookeeper

# ── Registry ──────────────────────────────────────────────────────────────────
# The client references the same registry the provider published into. etcdv3 is
# dubbo-go's etcd-v3 driver. Matches docker-compose.yml.
spring.dubbo.registries.zookeeper.protocol=zookeeper
spring.dubbo.registries.zookeeper.address=127.0.0.1:2181

# ── Consumer (client role) ────────────────────────────────────────────────────
# starter-dubbo builds one named *client.Client bean per entry under
# ${spring.dubbo.client}; the key ("greet") becomes the bean name. Protocol
# matches the provider's Triple. The named "greet" client is what
# StarterDubbo.RegisterReference("greet", ...) wires into the stub bean.
spring.dubbo.client.greet.protocol=tri
