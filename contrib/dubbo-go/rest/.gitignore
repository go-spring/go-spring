# Runtime observability artifacts written by the provider at runtime (JSON
# logs tailed by Promtail). Not source; regenerated on every
# scripts/smoke-test.sh run. Only the provider is instrumented in this module,
# so only provider.log lands here.
logs/
