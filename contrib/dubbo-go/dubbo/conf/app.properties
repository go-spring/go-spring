# The built-in HTTP server is disabled so the provider exposes only the Dubbo
# endpoint below. Without this, gs.Run() would try to bind an HTTP listener on
# the default port and complain that no handlers are registered.
spring.http.server.enabled=false

# Dubbo server wired by the starter-dubbo module. Protocols are map-driven
# under ${spring.dubbo.server.protocols}: the key is the dubbo-go protocol
# name and only configured protocols are enabled. Here the classic Dubbo
# (TCP + Hessian2) protocol on port 20001, chosen so this module can coexist
# with the Triple sibling (20000) on the same host.
spring.dubbo.server.protocols.dubbo.port=20001

# etcd registry, map-driven under ${spring.dubbo.server.registries}: the key
# is the dubbo-go registry name. The provider publishes com.example.GreetService
# here on startup; the consumer resolves a live provider address from the same
# registry instead of dialing a hard-coded host:port. Matches docker-compose.yml.
spring.dubbo.server.registries.etcdv3.address=127.0.0.1:2379
