apiVersion: v2
name: GS_APP_NAME
description: Helm chart for GS_PROJECT_NAME (generated by gs k8s)
type: application
# Chart version; bump on chart changes.
version: 0.1.0
# Default app version; overridden by image.tag / --set image.tag.
appVersion: "latest"
